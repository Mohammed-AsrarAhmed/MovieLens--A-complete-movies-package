services:
  - type: web
    name: movielens
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: MONGODB_URI
        value: "mongodb+srv://your-user:your-password@cluster.mongodb.net"
